diff --git a/src/Manager/UserManager.php b/src/Manager/UserManager.php
index 2359ee6..844bbd2 100644
--- a/src/Manager/UserManager.php
+++ b/src/Manager/UserManager.php
@@ -4,18 +4,26 @@ namespace App\Manager;
 
 use App\Entity\User;
 use Doctrine\ORM\EntityManagerInterface;
+use Symfony\Component\Mailer\MailerInterface;
 use Symfony\Component\Routing\Generator\UrlGeneratorInterface;
 use Symfony\Component\Routing\RouterInterface;
 use Symfony\Component\Security\Core\Encoder\UserPasswordEncoderInterface;
 
 class UserManager
 {
+    private $mailer;
     private $router;
     private $passwordEncoder;
     private $entityManager;
 
-    public function __construct(RouterInterface $router, UserPasswordEncoderInterface $passwordEncoder, EntityManagerInterface $entityManager)
+    public function __construct(
+        MailerInterface $mailer,
+        RouterInterface $router,
+        UserPasswordEncoderInterface $passwordEncoder,
+        EntityManagerInterface $entityManager
+    )
     {
+        $this->mailer = $mailer;
         $this->router = $router;
         $this->passwordEncoder = $passwordEncoder;
         $this->entityManager = $entityManager;
