{% extends 'base.html.twig' %}

{% block body %}
<div class="col-8">
    <h3>Recently Reported Sightings</h3>
    <small>
        <a class="btn-link" href="{{ path('app_sighting_upload') }}">Submit </a>
        yours to help us narrow down this search
    </small>

    <table class="table table-striped table-dark table-borderless table-hover">
        <thead>
            <tr class="bg-info">
                <th>Sighting Details</th>
                <th>Posted</th>
                <th>Comments</th>
            </tr>
        </thead>
        <tbody>
            {{ include('main/_sightings.html.twig') }}
        </tbody>
    </table>
</div>
<div class="col-4">
    <h3>Most Active Sightings</h3>
    <small>
        some text here
    </small>
    <table class="table table-striped table-dark table-borderless table-hover">
        <thead>
        <tr class="bg-info">
            <th>Sighting Details</th>
            <th>Posted</th>
            <th>Comments</th>
        </tr>
        </thead>
        <tbody>
        {{ include('main/_sightings.html.twig', {
            sightings: mostActiveSightings
        }) }}
        </tbody>
    </table>
</div>

{% endblock %}