{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bigfoot is out there!</h1>

    <h3>Sightings:</h3>
    <div class="js-sightings-list" data-url="{{ path('app_sightings_partial_list') }}">
        {{ include('main/_sightings.html.twig') }}
    </div>
{% endblock %}