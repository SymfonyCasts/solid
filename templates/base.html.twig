<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Finding Bigfoot{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <body>

        {% if is_granted('ROLE_USER') %}
            <a href="{{ path('app_logout') }}">Log Out</a>
        {% else %}
            <a href="{{ path('app_login') }}">Log In</a>
        {% endif %}
        <a href="{{ path('app_about') }}">About Us</a>

        <div class="container">
            <div class="row">
                <div class="col-12 mb-5 top-nav">
                    <h1 class="mt-5"><img class="mr-3" src="{{ asset('build/images/bigfoot.png') }}" alt="bigfoot icon" style="width:50px;height:65px;">Sasquatch Sightings</h1>
                </div>
            </div>
            <div class="row">
                <div class="col-12">
                    {% block body %}{% endblock %}
                </div>
            </div>
            <div class="row">
                <div class="col-12 footer-nav">
                    <p class="pt-4">Made with <span class="text-danger"><3</span> by the guys and gals at <u><a class="text-white" href="https://symfonycasts.com">SymfonyCasts</a></u></p>
                </div>
            </div>
        </div>

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </body>
</html>
